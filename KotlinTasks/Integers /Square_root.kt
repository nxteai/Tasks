import kotlin.math.sqrt

fun main() {
    val number = readln().toDouble()
    println(sqrt(number))
}
/* This Kotlin code reads a number from the user, converts it to a double, 
and then prints the square root of the number using the sqrt() function from the kotlin.math library. 
It simplifies taking user input and immediately calculating and displaying the square root of the inputted value. *\
